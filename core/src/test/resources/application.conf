test {
  cassandra {
    seeds = ["localhost"]
    port = 9042

    keyspace = "dapper"

    ssl {
      enabled = false
    }

    credentials {
      enabled = true
      username = "cassandra"
      password = "cassandra"
    }
  }
}